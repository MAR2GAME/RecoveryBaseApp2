<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/config"
        android:usesCleartextTraffic="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo_2026"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.PhotoRecovery">
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${advAdmobAppId}" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="${advFacebookAppId}" />

        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="${advFacebookClientToken}" />

        <service
            android:name="com.pdffox.adv.notification.CommonService"
            android:enabled="${advPersistentServiceEnabled}"
            tools:replace="android:enabled" />

        <service
            android:name="com.pdffox.adv.push.MyFirebaseMessagingService"
            android:enabled="${advFirebaseMessagingServiceEnabled}"
            tools:replace="android:enabled" />

        <service
            android:name="com.pdffox.adv.push.ServiceStarterJobService"
            android:enabled="${advServiceStarterJobEnabled}"
            tools:replace="android:enabled" />

        <receiver
            android:name="com.pdffox.adv.notification.NotificationDeletedReceiver"
            android:enabled="${advNotificationDeletedReceiverEnabled}"
            tools:replace="android:enabled" />

        <receiver
            android:name="com.pdffox.adv.notification.BootReceiver"
            android:enabled="${advBootReceiverEnabled}"
            tools:replace="android:enabled" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.pdffox.adv.fileprovider"
            android:enabled="${advFileProviderEnabled}"
            tools:replace="android:enabled" />

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PhotoRecovery">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="OPEN_MAIN_ACTIVITY" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>
